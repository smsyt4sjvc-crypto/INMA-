# RAW — INMA baseline ingest (provenance record)
_Saved 2026-07-10. Evidence locker entry — not edited after this._

This is the source-of-record for the vault's founding memory notes. Rather than
duplicate large files, it points to the canonical sources they were built from,
all version-controlled in this repo.

## Sources ingested
1. **Repo-root `CLAUDE.md`** — INMA business brain: positioning (§2), money model
   (§3, §3a), ways to work (§4), verbiage rules (§5–6), voice (§7), brand/visual
   system (§8), document brand kit (§8a), site & tools (§9), contact block (§10).
   This is the authoritative business-facts document; the vault interprets and
   tracks evolution around it.
2. **This session's build work on `main`** (2026-07-10), commits through `ab68f86`
   and the vault add `5619119`:
   - Builders got reliable Back buttons; iOS octet-stream export.
   - `field-estimate.html` gained autosave + "+ New" + offer-page card metadata.
   - `offers/index.html` rebuilt to auto-list `offer-*.html` via the public GitHub
     contents API (dropped the Google-Sheet dependency).
   - Offer folder cleared of test files for a clean start.
   - Accept-checkbox iOS render bug fixed.

## Notes produced from this ingest
[[architecture]] · [[brand]] · [[positioning]] · [[evolution]] · [[offer-board]] ·
[[state]]
